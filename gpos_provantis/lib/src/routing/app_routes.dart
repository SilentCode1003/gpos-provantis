/// Defines all the route names in the app as an enum to prevent typos.
enum AppRoute {
  employees,
  login,
  setup,
  dashboard,
  settings,
  startup,
  cashReports,
  soldItems,
  sendEreceipt,
  receipts,
  refunds,
  reprint,
  reports,
}
