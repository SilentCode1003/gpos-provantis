import 'package:drift/drift.dart';
import '../app_database.dart';

/// Version 2: Added Users Table
Future<void> migrateV2(Migrator m, AppDatabase db) async {
  // await m.createTable(db.userDataTable);
  // await m.createTable(db.pOSConfigTable);
}
